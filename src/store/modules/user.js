import Vue from 'vue'
import api from '../../api/api'
import * as types from "../mutation-types"

const user = {
  state: {
    
  },
  
  mutations: {
  },
  
  actions: {
    
  }
  
};

export default user